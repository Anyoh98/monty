#include "monty.h"

/**
 * nop_op - Should do nothing
 * @head: pointer to address of the top of the stack
 * @line_number: line number of the opcode
 **/
void nop_op(stack_t **head, unsigned int line_number)
{
	(void)head;
	(void)line_number;
}
